# captainlog-marketi

Marketing-Website für Captain's Log (Harbor Studios). Build: `python3 build_static.py` erzeugt `dist/` aus den `.dc.html`-Quellen. Deployed via Vercel (Output Directory: `dist`).ng
